name: Multi Shell Commands # here we create only 1 job
on: workflow_dispatch
jobs:
  multi-shells: #job name
    runs-on: ubuntu-latest
    steps:
       - name: Hello Step
         run: |
           echo “This is line one”
           echo “This is line two”
