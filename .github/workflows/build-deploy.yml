name: Build & Deploy
on: 
  pull_request: 
    types:
    - opened
    - synchronize
jobs:
  build:
    runs-on: ubuntu-latest #ubuntu runner
    steps:
      - name: Build Step
        run: echo "Building the application"
