name: Hello World # here we create only 1 job
on: workflow_dispatch
jobs: 
  hello_job_1: #job name
    runs-on: ubuntu-latest
    steps:
      - name: Print welcome message
        run: echo “Welcome to GHA”
